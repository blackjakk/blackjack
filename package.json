{
  "name": "megaeth-blackjack",
  "private": true,
  "version": "0.1.0",
  "description": "Play-money onchain blackjack MVP for MegaETH testnet. NOT audited. NOT for real money.",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
